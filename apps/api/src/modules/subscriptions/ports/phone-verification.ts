/**
 * Гейт подтверждения телефона (вход в триал). Источник правды — флоу верификации звонком
 * (модуль identity/tenancy). Порт объявлен здесь (потребитель владеет портом, гексагон):
 * подписки не должны доверять клиентскому «телефон подтверждён» — проверяем на сервере.
 */
export type PhoneVerificationGate = {
  readonly isVerified: (phoneE164: string) => Promise<boolean>;
};
